name: 'Editor Config format check'

on:
  pull_request:
    branches: [future/prototype]

permissions:
  contents: read

jobs:
  validate:
    name: Validate Editor Config formatting
    runs-on: 'ubuntu-latest'
    steps:
    - name: Checkout PR head branch
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
        ref: ${{ github.event.pull_request.head.ref }}
    - name: Editor Config Validation
      uses: github/super-linter/slim@v4
      env:
        VALIDATE_ALL_CODEBASE: false
        VALIDATE_EDITORCONFIG: true
        DEFAULT_BRANCH: ${{ github.event.pull_request.base.sha }}
        GITHUB_WORKSPACE: '${{ env.GITHUB_WORKSPACE }}/prototype'
        LOG_LEVEL: TRACE
